import { Rubik, Roboto } from "next/font/google";

export const rubik = Rubik({
  subsets: ["latin"],
  display: "swap",
});

export const roboto = Roboto({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
